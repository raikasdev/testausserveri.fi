import { PostFeatured3D } from './PostFeatured3D';

export default {
  title: 'PostFeatured3D',
  component: PostFeatured3D,
}

const Template = (args) => <PostFeatured3D {...args} />

export const Default = {
  args: {
  }
}